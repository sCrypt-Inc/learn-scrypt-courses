contract TicTacToe {
    PubKey alice;
    PubKey bob;
    
    static const int TURNLEN = 1;
    static const int BOARDLEN = 9;
    static const bytes EMPTY = b'00';
    static const bytes ALICE = b'01';
    static const bytes BOB = b'02';

    
    function move(int n, Sig sig, int amount, SigHashPreimage txPreimage) {

    }


    function won(bytes board, bytes play) {

    }


    function full(bytes board) {

    }
}
