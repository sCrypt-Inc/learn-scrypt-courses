
contract MyHelloWorld {

  public function unlock(int x) {

    bool done = false;
    loop (3) {
      if (!done) {
        x = x * 2;
        if (x >= 8) {
            done = true;
        }
      }
    }

    require(true);
  }
}
