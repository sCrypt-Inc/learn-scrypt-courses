
contract MyHelloWorld {

  public function unlock(int x) {

    bool done = false;
    loop (3) {
      if (!done) {
        x = x * 2;
        //TODO: check if you need to break out of the loop
      }
    }

    require(true);
  }
}
